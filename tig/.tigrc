# Open the file in the Finder with "=".
bind generic = @open --reveal %(file)

set wrap-lines = yes
set diff-highlight = yes
set git-colors = no

# --------------------------------------------------------------
# Appearance.
# --------------------------------------------------------------

color cursor default green
