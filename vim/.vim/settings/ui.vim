" UI stuffs
" ######### 

" ------------------------------

" Hide startup message (the `I` flag disables the message).
set shortmess+=I

" ------------------------------

if has('gui_running')

  " Font.
	set guifont=Menlo\ Regular\ for\ Powerline:h14

else

endif
